version: 2
frontend:
  phases:
    build:
      commands:
        - echo "No build commands required for static site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
