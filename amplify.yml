version: 2
backend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
        - amplifyPush --simple
frontend:
  phases:
    build:
      commands:
        - echo "No build required for static site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
